# [5.6] Multimodal, Embedding, and Function-Calling Models

> **Local-first extension.** This section builds the small invariants behind embedding retrieval, representation probes, and function-calling validation before running pinned released checkpoints on CUDA.

```yaml
gt_tier: GT-1 with GT-0 controlled metric exercises
exercise_id: 5.6-multimodal-embedding-function-models
expected_runtime: 45-75 minutes for CPU exercises; several minutes for CUDA embedding and FunctionGemma preflights
requires_gpu: true for the released-checkpoint preflight; false for the implementation exercises
```

Please send any problems / bugs on the `#errata` channel in the [Slack group](https://info-arena.github.io/ARENA_img/slack.html), and ask any questions on the dedicated channels for this chapter of material.

If you want to change to dark mode, you can do this by clicking the three horizontal lines in the top-right, then navigating to Settings -> Theme.

Links to earlier chapters: [(0) Fundamentals](https://arena-chapter0-fundamentals.streamlit.app/), [(1) Transformer Interpretability](https://arena-chapter1-transformer-interp.streamlit.app/), [(2) RL](https://arena-chapter2-rl.streamlit.app/), [(3) LLM Evaluations](https://arena-chapter3-llm-evals.streamlit.app/), [(4) Alignment Science](https://arena-chapter4-alignment-science.streamlit.app/).

<img src="https://raw.githubusercontent.com/info-arena/ARENA_img/main/misc/headers/header-13-2.png" width="350">


# Introduction

The previous architecture sections focused on token generators: Gemma-style decoders, Mamba-style recurrent models, and diffusion language models. The roadmap also calls out specialist model families that do not fit the ordinary next-token notebook template:

* **Embedding models** map text into a geometry used for retrieval, clustering, probes, and semantic search.
* **Multimodal encoders** align image, text, audio, or video representations in a shared space.
* **Function-calling models** must choose a valid tool, abstain when no tool is appropriate, and fill schema fields without inventing unsupported arguments.

This notebook is deliberately metric-first. A nearest-neighbor example that looks plausible is not enough. A generated tool call that looks syntactically valid is not enough. You will build the checks that catch hard negatives, invalid tools, no-call hallucinations, and schema-token hypotheses before trusting real checkpoint output.

The checked CUDA path uses `BAAI/bge-small-en-v1.5` as a public embedding baseline, directly loads authenticated `google/embeddinggemma-300m` for retrieval, evaluates `litert-community/FunctionGemma_270M_Mobile_Actions` on held-out `google/mobile-actions` eval rows, and directly loads authenticated base `google/functiongemma-270m-it` for a benign forward-pass preflight.


## Content & Learning Objectives

### 1. Embedding pooling and retrieval

> ##### Learning Objectives
>
> * Mean-pool only unpadded token positions.
> * Compute paired retrieval accuracy and mean reciprocal rank.
> * Use hard-negative margins rather than only inspecting nearest neighbors.

### 2. Representation probes

> ##### Learning Objectives
>
> * Fit a simple nearest-centroid probe.
> * Validate embedding geometry on held-out points.
> * Keep the probe simple enough that failures are easy to diagnose.

### 3. Function-call validity

> ##### Learning Objectives
>
> * Enforce a tool schema before selecting a tool.
> * Separate wrong-tool errors from no-call hallucinations.
> * Parse generated FunctionGemma calls into names and schema arguments.

### 4. Schema-token attribution

> ##### Learning Objectives
>
> * Project hidden states onto schema-token directions.
> * Treat attribution as a hypothesis generator, not causal proof.
> * Use attribution to choose follow-up probes, ablations, or steering tests.


## Setup code

```python
import re
import sys
from dataclasses import dataclass
from pathlib import Path

import torch as t

chapter = "chapter5_modern_architectures"
section = "part6_multimodal_embedding_function_models"
root_dir = next(p for p in [Path.cwd(), *Path.cwd().parents] if (p / chapter).exists())
exercises_dir = root_dir / chapter / "exercises"
section_dir = exercises_dir / section

if str(root_dir) not in sys.path:
    sys.path.append(str(root_dir))
if str(exercises_dir) not in sys.path:
    sys.path.append(str(exercises_dir))

import part6_multimodal_embedding_function_models.tests as tests
import part6_multimodal_embedding_function_models.utils as utils

MAIN = __name__ == "__main__"
```

```python
@dataclass(frozen=True)
class EmbeddingRetrievalReport:
    top1_accuracy: float
    mean_reciprocal_rank: float
    mean_positive_similarity: float
    mean_hard_negative_similarity: float
    mean_margin: float


@dataclass(frozen=True)
class CentroidProbe:
    labels: t.Tensor
    centroids: t.Tensor


@dataclass(frozen=True)
class FunctionCallReport:
    accuracy: float
    tool_accuracy: float
    abstention_accuracy: float
    hallucination_rate: float


@dataclass(frozen=True)
class ParsedFunctionCall:
    name: str | None
    arguments: dict[str, str]
```


# Embedding Retrieval

### Exercise - mean-pool unpadded embeddings

> ```yaml
> Difficulty: easy
> Importance: high
>
> You should spend up to 5-10 mins on this exercise.
> ```

Embedding models often return one vector per token. A common sentence-level baseline is masked mean pooling. Padding tokens must not affect the pooled representation.

Expected output: the test should return `[[2.0, 1.0], [4.0, 6.0]]` even though the padded token contains a large sentinel vector.

Common bug: summing over the whole sequence and dividing by `seq_len`, which lets padding leak into the sentence embedding.

```python
def mean_pool_embeddings(token_embeddings: t.Tensor, attention_mask: t.Tensor) -> t.Tensor:
    raise NotImplementedError()


tests.test_mean_pool_embeddings_ignores_padding_and_matches_reference(mean_pool_embeddings)
```

<details>
<summary>Solution</summary>

```python
def mean_pool_embeddings(token_embeddings: t.Tensor, attention_mask: t.Tensor) -> t.Tensor:
    if token_embeddings.ndim != 3:
        raise ValueError("token_embeddings must have shape (batch, seq, dim).")
    if attention_mask.shape != token_embeddings.shape[:2]:
        raise ValueError("attention_mask must have shape (batch, seq).")

    mask = attention_mask.to(device=token_embeddings.device, dtype=token_embeddings.dtype)
    weighted = token_embeddings * mask.unsqueeze(-1)
    denom = mask.sum(dim=-1, keepdim=True).clamp_min(1)
    return weighted.sum(dim=1) / denom
```
</details>


### Exercise - paired retrieval with hard negatives

> ```yaml
> Difficulty: medium
> Importance: high
>
> You should spend up to 10-15 mins on this exercise.
> ```

For a text-image or query-document embedding model, paired retrieval is the first sanity check. Each query should rank its paired candidate above all negatives. The margin to the strongest negative matters because a high top-1 result can still be brittle.

Expected output: the controlled test includes one hard negative, so the ranks should be `[1, 2, 1]` and top-1 accuracy should be `2/3`.

Common bug: computing `query_embeddings @ candidate_embeddings.T` without normalizing, which turns the metric into a norm probe.

```python
def l2_normalize(x: t.Tensor, *, eps: float = 1e-12) -> t.Tensor:
    raise NotImplementedError()


def cosine_similarity_matrix(
    query_embeddings: t.Tensor,
    candidate_embeddings: t.Tensor,
) -> t.Tensor:
    raise NotImplementedError()


def retrieval_ranks(similarity: t.Tensor, target_indices: t.Tensor) -> t.Tensor:
    raise NotImplementedError()


def embedding_retrieval_report(
    query_embeddings: t.Tensor,
    candidate_embeddings: t.Tensor,
    target_indices: t.Tensor,
) -> EmbeddingRetrievalReport:
    raise NotImplementedError()


tests.test_retrieval_metrics_rank_pairs_and_hard_negative_margin(
    cosine_similarity_matrix,
    retrieval_ranks,
    embedding_retrieval_report,
)
```

<details>
<summary>Solution</summary>

```python
def l2_normalize(x: t.Tensor, *, eps: float = 1e-12) -> t.Tensor:
    if x.ndim == 0:
        raise ValueError("x must have at least one dimension.")
    return x / x.norm(dim=-1, keepdim=True).clamp_min(eps)


def cosine_similarity_matrix(
    query_embeddings: t.Tensor,
    candidate_embeddings: t.Tensor,
) -> t.Tensor:
    if query_embeddings.ndim != 2 or candidate_embeddings.ndim != 2:
        raise ValueError("embeddings must have shape (items, dim).")
    if query_embeddings.shape[1] != candidate_embeddings.shape[1]:
        raise ValueError("query and candidate embedding dimensions must match.")
    return l2_normalize(query_embeddings) @ l2_normalize(candidate_embeddings).T


def retrieval_ranks(similarity: t.Tensor, target_indices: t.Tensor) -> t.Tensor:
    if similarity.ndim != 2:
        raise ValueError("similarity must have shape (queries, candidates).")
    if target_indices.shape != (similarity.shape[0],):
        raise ValueError("target_indices must have shape (queries,).")
    if target_indices.min() < 0 or target_indices.max() >= similarity.shape[1]:
        raise ValueError("target_indices contains an invalid candidate index.")

    order = similarity.argsort(dim=-1, descending=True)
    matches = order.eq(target_indices[:, None])
    return matches.float().argmax(dim=-1).long() + 1


def embedding_retrieval_report(
    query_embeddings: t.Tensor,
    candidate_embeddings: t.Tensor,
    target_indices: t.Tensor,
) -> EmbeddingRetrievalReport:
    similarity = cosine_similarity_matrix(query_embeddings, candidate_embeddings)
    ranks = retrieval_ranks(similarity, target_indices)
    row = t.arange(similarity.shape[0], device=similarity.device)
    positive = similarity[row, target_indices]

    if similarity.shape[1] == 1:
        hard_negative = positive.new_zeros(positive.shape)
    else:
        masked_similarity = similarity.clone()
        masked_similarity[row, target_indices] = -t.inf
        hard_negative = masked_similarity.max(dim=-1).values

    return EmbeddingRetrievalReport(
        top1_accuracy=ranks.eq(1).float().mean().item(),
        mean_reciprocal_rank=(1.0 / ranks.float()).mean().item(),
        mean_positive_similarity=positive.mean().item(),
        mean_hard_negative_similarity=hard_negative.mean().item(),
        mean_margin=(positive - hard_negative).mean().item(),
    )
```
</details>


# Representation Probes

### Exercise - fit a centroid probe

> ```yaml
> Difficulty: medium
> Importance: high
>
> You should spend up to 10-15 mins on this exercise.
> ```

Nearest-centroid probes are weak but useful. If a simple centroid probe cannot recover a known synthetic concept, the embedding geometry or labels are probably wrong.

Expected output: held-out cluster points should be classified with accuracy `1.0`.

Common bug: reporting training accuracy only, which hides whether the geometry generalizes.

```python
def fit_centroid_probe(embeddings: t.Tensor, labels: t.Tensor) -> CentroidProbe:
    raise NotImplementedError()


def predict_centroid_probe(embeddings: t.Tensor, probe: CentroidProbe) -> t.Tensor:
    raise NotImplementedError()


def centroid_probe_accuracy(embeddings: t.Tensor, labels: t.Tensor, probe: CentroidProbe) -> float:
    raise NotImplementedError()


tests.test_centroid_probe_recovers_heldout_clusters(
    fit_centroid_probe,
    predict_centroid_probe,
    centroid_probe_accuracy,
)
```

<details>
<summary>Solution</summary>

```python
def fit_centroid_probe(embeddings: t.Tensor, labels: t.Tensor) -> CentroidProbe:
    if embeddings.ndim != 2:
        raise ValueError("embeddings must have shape (items, dim).")
    if labels.shape != (embeddings.shape[0],):
        raise ValueError("labels must have shape (items,).")

    unique_labels = labels.unique(sorted=True)
    centroids = t.stack([embeddings[labels == label].mean(dim=0) for label in unique_labels])
    return CentroidProbe(labels=unique_labels, centroids=l2_normalize(centroids))


def predict_centroid_probe(embeddings: t.Tensor, probe: CentroidProbe) -> t.Tensor:
    similarity = l2_normalize(embeddings) @ probe.centroids.to(embeddings.device).T
    centroid_ids = similarity.argmax(dim=-1)
    return probe.labels.to(embeddings.device)[centroid_ids]


def centroid_probe_accuracy(embeddings: t.Tensor, labels: t.Tensor, probe: CentroidProbe) -> float:
    predictions = predict_centroid_probe(embeddings, probe)
    return predictions.eq(labels).float().mean().item()
```
</details>


# Function-Call Validation

### Exercise - mask disallowed tools

> ```yaml
> Difficulty: easy
> Importance: high
>
> You should spend up to 5-10 mins on this exercise.
> ```

Tool schemas are part of the behavior contract. If a tool is not available in the current context, its logit should not be eligible for selection.

Expected output: a high-logit but disallowed tool should become `-inf`, and the prediction should move to the best allowed tool.

Common bug: masking after `argmax`; at that point the invalid tool has already won.

```python
def mask_disallowed_tools(logits: t.Tensor, allowed_tools: t.Tensor) -> t.Tensor:
    raise NotImplementedError()


tests.test_mask_disallowed_tools_blocks_invalid_logits(mask_disallowed_tools)
```

<details>
<summary>Solution</summary>

```python
def mask_disallowed_tools(logits: t.Tensor, allowed_tools: t.Tensor) -> t.Tensor:
    if logits.ndim != 2:
        raise ValueError("logits must have shape (batch, tools).")
    if allowed_tools.shape == (logits.shape[1],):
        allowed = allowed_tools.to(device=logits.device, dtype=t.bool).expand_as(logits)
    elif allowed_tools.shape == logits.shape:
        allowed = allowed_tools.to(device=logits.device, dtype=t.bool)
    else:
        raise ValueError("allowed_tools must have shape (tools,) or (batch, tools).")
    return logits.masked_fill(~allowed, -t.inf)
```
</details>


### Exercise - measure tool hallucination

> ```yaml
> Difficulty: medium
> Importance: high
>
> You should spend up to 10-15 mins on this exercise.
> ```

A function-calling model needs no-call examples. Otherwise a model can look strong by always picking some plausible tool.

Expected output: the report should separately expose overall accuracy, tool accuracy, abstention accuracy, and hallucination rate.

Common bug: computing one accuracy over all labels and missing that the model hallucinates tools on no-call prompts.

```python
def function_call_report(
    logits: t.Tensor,
    labels: t.Tensor,
    *,
    no_call_id: int,
) -> FunctionCallReport:
    raise NotImplementedError()


tests.test_function_call_report_separates_tool_and_abstention_errors(function_call_report)
```

<details>
<summary>Solution</summary>

```python
def function_call_report(
    logits: t.Tensor,
    labels: t.Tensor,
    *,
    no_call_id: int,
) -> FunctionCallReport:
    if logits.ndim != 2:
        raise ValueError("logits must have shape (batch, tools).")
    if labels.shape != (logits.shape[0],):
        raise ValueError("labels must have shape (batch,).")
    if not 0 <= no_call_id < logits.shape[1]:
        raise ValueError("no_call_id is out of range.")

    predictions = logits.argmax(dim=-1)
    tool_mask = labels.ne(no_call_id)
    abstain_mask = labels.eq(no_call_id)
    accuracy = predictions.eq(labels).float().mean().item()

    tool_accuracy = (
        predictions[tool_mask].eq(labels[tool_mask]).float().mean().item()
        if tool_mask.any()
        else float("nan")
    )
    abstention_accuracy = (
        predictions[abstain_mask].eq(no_call_id).float().mean().item()
        if abstain_mask.any()
        else float("nan")
    )
    hallucination_rate = (
        predictions[abstain_mask].ne(no_call_id).float().mean().item()
        if abstain_mask.any()
        else float("nan")
    )
    return FunctionCallReport(accuracy, tool_accuracy, abstention_accuracy, hallucination_rate)
```
</details>


### Exercise - parse FunctionGemma call text

> ```yaml
> Difficulty: medium
> Importance: high
>
> You should spend up to 10-15 mins on this exercise.
> ```

FunctionGemma-style generations can contain a structured span like `call:open_app{app:<escape>Google Maps<escape>, mode:navigation}`. You need a parser before you can score tool name and argument accuracy.

Expected output: the parser should recover `name == "open_app"` and `arguments == {"app": "Google Maps", "mode": "navigation"}`.

Common bug: using a JSON parser on text that is not JSON, or dropping escaped argument values containing spaces.

```python
_FUNCTION_CALL_RE = re.compile(r"call:([A-Za-z0-9_]+)\{([^}]*)\}")
_FUNCTION_ARG_RE = re.compile(r"([A-Za-z0-9_]+):(?:<escape>(.*?)<escape>|([^,{}]+))")


def parse_function_call_text(text: str) -> ParsedFunctionCall:
    raise NotImplementedError()


tests.test_parse_function_call_text_extracts_name_and_arguments(parse_function_call_text)
```

<details>
<summary>Solution</summary>

```python
def parse_function_call_text(text: str) -> ParsedFunctionCall:
    match = _FUNCTION_CALL_RE.search(text)
    if match is None:
        return ParsedFunctionCall(name=None, arguments={})

    arguments: dict[str, str] = {}
    for arg_match in _FUNCTION_ARG_RE.finditer(match.group(2)):
        escaped_value, bare_value = arg_match.group(2), arg_match.group(3)
        value = escaped_value if escaped_value is not None else bare_value
        arguments[arg_match.group(1)] = value.strip()
    return ParsedFunctionCall(name=match.group(1), arguments=arguments)
```
</details>


# Schema Attribution

### Exercise - project onto schema-token directions

> ```yaml
> Difficulty: medium
> Importance: medium
>
> You should spend up to 5-10 mins on this exercise.
> ```

Schema-token attribution is a hypothesis generator: it can suggest which schema fields or tool tokens a hidden state aligns with. It is not a substitute for held-out validation or causal intervention.

Expected output: the test checks that attribution is exactly the matrix of hidden-state dot products with schema vectors.

Common bug: applying softmax and treating the result as causal attribution. Keep this as a signed compatibility score.

```python
def schema_token_attribution(hidden_states: t.Tensor, schema_vectors: t.Tensor) -> t.Tensor:
    raise NotImplementedError()


tests.test_schema_token_attribution_matches_dot_products(schema_token_attribution)
```

<details>
<summary>Solution</summary>

```python
def schema_token_attribution(hidden_states: t.Tensor, schema_vectors: t.Tensor) -> t.Tensor:
    if hidden_states.shape[-1] != schema_vectors.shape[-1]:
        raise ValueError("hidden state and schema vector dimensions must match.")
    return hidden_states @ schema_vectors.T
```
</details>


# Full Experiment Path

The full specialist-model experiment should load one released checkpoint from the family you want to study:

* an embedding model for query-document or multilingual retrieval,
* a CLIP/SigLIP-style text-image encoder,
* a small VLM with a vision encoder and language decoder,
* or a function-calling model with a concrete tool schema.

For each model:

1. Define paired positives, hard negatives, and no-call or no-match controls.
2. Run the model locally with fixed seeds and explicit dtype or quantization settings.
3. Save embeddings, logits, tool decisions, prompts, and metadata.
4. Report retrieval accuracy, mean reciprocal rank, hard-negative margin, and probe accuracy.
5. Report tool-choice accuracy, abstention accuracy, and hallucination rate.
6. Capture activations at the layer where you plan to interpret schema or modality features.
7. Compare against text-only, random-vector, shuffled-pair, or invalid-tool baselines.

The checked GPU path for this notebook uses:

* `BAAI/bge-small-en-v1.5` revision `5c38ec7c405ec4b44b94cc5a9bb96e735b38267a` on four generated query/document pairs, plus a permuted-pair negative control.
* `google/embeddinggemma-300m` revision `57c266a740f537b4dc058e1b0cda161fd15afa75` on the same controlled retrieval pairs, with matching permuted-pair controls.
* `litert-community/FunctionGemma_270M_Mobile_Actions` revision `e2226c1def35c5443942ebdb90a1da2a9eda836a` on the deterministic first 32 held-out `google/mobile-actions` eval rows from dataset revision `e920309bc2acbc2e99a5e3201cf37df2b9fd9151`.
* `google/functiongemma-270m-it` revision `39eccb091651513a5dfb56892d3714c1b5b8276c` for direct authenticated CUDA loading and a benign forward-pass check.

The FunctionGemma path reports parse accuracy, function-name accuracy, exact argument accuracy, required-argument accuracy, failure indices, dtype/device context from the model loader, and peak VRAM. It does not hide imperfect argument generation: the current acceptance threshold requires perfect parsing and function-name routing, plus at least 85% exact and required-argument accuracy on the deterministic eval slice.

EmbeddingGemma and the base `google/functiongemma-270m-it` repo are manual-gated artifacts, so the verification report records authentication/readiness metadata before claiming the direct CUDA checks.

The minimum acceptance criteria are:

```text
masked pooling ignores padding
paired retrieval beats hard negatives or reports the miss
held-out centroid probe beats random labels
tool masking prevents disallowed choices
no-call prompts are included and hallucination rate is reported
FunctionGemma text calls parse into tool names and arguments
schema-token attribution is used only as a hypothesis generator
released checkpoint path records pinned revisions and VRAM budget
FunctionGemma eval records parse/name/argument accuracy on held-out rows
```


# Notebook Contract

This section exposes the standard extension entry points:

```python
def run_smoke_test(cpu: bool = True) -> dict:
    ...


def run_gpu_test(max_vram_gb: float = 24.0) -> dict:
    ...


def run_full_experiment(max_vram_gb: float = 24.0) -> dict:
    ...
```

The smoke test passes only if:

```text
mean pooling ignores padded tokens
paired retrieval has perfect top-1 accuracy on synthetic pairs
centroid probe generalizes to held-out synthetic points
tool masking blocks a high-logit disallowed tool
function-call report catches a no-call hallucination
schema attribution ranks the expected schema directions
```

The next section should move from architecture-specific model families into sparse feature methods: SAE variants, released feature artifacts, transcoders, attribution graphs, and crosscoders.
