# [5.5] Toy Discrete Diffusion Language Models and Local DiffusionGemma Proof


> **Local-first extension.** This section introduces discrete diffusion language models. You will implement toy noising, masked denoising loss, confidence remasking, iterative sampling, and denoising-step diagnostics, then verify the mechanics with a CUDA-trained tiny conditional diffusion LM on a generated copy-pair grammar.

Please send any problems / bugs on the `#errata` channel in the [Slack group](https://info-arena.github.io/ARENA_img/slack.html), and ask any questions on the dedicated channels for this chapter of material.

If you want to change to dark mode, you can do this by clicking the three horizontal lines in the top-right, then navigating to Settings -> Theme.

Links to earlier chapters: [(0) Fundamentals](https://arena-chapter0-fundamentals.streamlit.app/), [(1) Transformer Interpretability](https://arena-chapter1-transformer-interp.streamlit.app/), [(2) RL](https://arena-chapter2-rl.streamlit.app/), [(3) LLM Evaluations](https://arena-chapter3-llm-evals.streamlit.app/), [(4) Alignment Science](https://arena-chapter4-alignment-science.streamlit.app/).

<img src="https://raw.githubusercontent.com/info-arena/ARENA_img/main/misc/headers/header-05.png" width="350">


## Notebook Metadata

```yaml
gt_tier: GT-0
exercise_id: 5_5_diffusion_language_models
expected_runtime: 60-90 minutes for CPU exercises; several minutes for the CUDA tiny-model preflight
requires_gpu: true for the trained tiny diffusion LM; false for the implementation exercises
```


# Reading Material

You should understand masked-language-model cross entropy and transformer encoder blocks
before starting this section. The roadmap target is a full DiffusionGemma-style local
inference and patching lab. This section now separates two claims: the toy diffusion
mechanics are executed end-to-end on CUDA, and the released DiffusionGemma path is
checked against exact public model revisions, loader support, local shard readiness, and
runtime compatibility. Config/tokenizer support alone is not accepted as generation
evidence.


# Introduction


Autoregressive language models generate one token after another. Discrete diffusion language models instead start from a corrupted or masked sequence and repeatedly denoise it. This changes both the implementation surface and the interpretability questions:

* The training objective is a masked denoising objective, not next-token prediction.
* The sampler revises many tokens across steps.
* Token confidence and entropy can collapse at different times for different positions.
* Intermediate denoising states become natural targets for activation capture and patching.

The checked GPU path trains a small bidirectional denoiser on generated integer-token
sequences. The report verifies held-out masked-token reconstruction, confidence-remasking
sampler exact match, denoising-step activation shapes, and a shuffled-label control. It
then checks pinned `google/diffusiongemma-26B-A4B-it` and
`nvidia/diffusiongemma-26B-A4B-it-NVFP4` readiness: Transformers must recognize
`DiffusionGemmaForBlockDiffusion`, the processor/tokenizer must load, exact revisions and
shard counts must match, and the report must say whether a real BF16 or NVFP4 generation
path is actually ready.

The ladder below is the claim boundary for this section. You will implement the
toy diffusion mechanics directly, see a tiny CUDA denoiser beat a shuffled-label
control, and inspect a real NVFP4 DiffusionGemma generation proof. You will not
claim diffusion-time mechanistic interpretability from released DiffusionGemma
until the runtime exposes the needed denoising-step activations.

<img src="../assets/diffusion_lm_evidence_ladder.svg" width="820">


## Content & Learning Objectives

### 1. Discrete noising and denoising loss

> ##### Learning Objectives
>
> * Implement a monotonic mask schedule.
> * Verify extreme noising timesteps against analytic expectations.
> * Compute masked-token cross entropy without leaking unmasked positions into the loss.

### 2. Remasking and iterative sampling

> ##### Learning Objectives
>
> * Distinguish uniform remasking from confidence-based remasking.
> * Track mask fraction and entropy across denoising steps.
> * Verify a toy sampler against an oracle denoiser.

### 3. Commitment-time diagnostics

> ##### Learning Objectives
>
> * Compute per-token commitment indices from a denoising trajectory.
> * Use edit distance as a simple sequence-level comparison.
> * Treat activation shape checks as the minimum contract before circuit analysis.

### 4. Trained diffusion LM path

> ##### Learning Objectives
>
> * Train a tiny denoiser on a generated conditional grammar.
> * Check held-out masked-token accuracy and sampler exact match.
> * Reject shuffled-label targets as a negative control.

### 5. DiffusionGemma readiness path

> ##### Learning Objectives
>
> * Distinguish model metadata readiness from real generation evidence.
> * Check exact DiffusionGemma revisions and required weight shards.
> * Explain why the NVFP4 checkpoint needs a compatible ModelOpt-aware Transformers or vLLM runtime rather than a silent fallback.


## Setup Code


```python
import sys
from dataclasses import dataclass
from pathlib import Path

import torch as t
import torch.nn as nn
import torch.nn.functional as F

chapter = "chapter5_modern_architectures"
section = "part5_diffusion_language_models"
root_dir = next(p for p in [Path.cwd(), *Path.cwd().parents] if (p / chapter).exists())
exercises_dir = root_dir / chapter / "exercises"
section_dir = exercises_dir / section

if str(root_dir) not in sys.path:
    sys.path.append(str(root_dir))
if str(exercises_dir) not in sys.path:
    sys.path.append(str(exercises_dir))

import part5_diffusion_language_models.tests as tests
import part5_diffusion_language_models.utils as utils

MAIN = __name__ == "__main__"


@dataclass(frozen=True)
class DiscreteDiffusionSchedule:
    mask_probs: t.Tensor
    mask_token_id: int

    @property
    def num_steps(self) -> int:
        return int(self.mask_probs.numel())


@dataclass(frozen=True)
class NoisingResult:
    noisy_tokens: t.Tensor
    mask: t.Tensor
    timesteps: t.Tensor


@dataclass(frozen=True)
class DenoisingStepStats:
    step: int
    mask_fraction: float
    mean_entropy: float
    committed_fraction: float
```


# Discrete Noising


### Exercise - build a mask schedule

> ```yaml
> Difficulty: easy
> Importance: high
>
> You should spend up to 5-10 mins on this exercise.
> ```

A discrete diffusion schedule tells us how much of the sequence should be masked at each noise level. We index the schedule from low noise to high noise.

```python
def linear_mask_schedule(
    num_steps: int,
    *,
    mask_token_id: int,
    min_mask_prob: float = 0.0,
    max_mask_prob: float = 1.0,
) -> DiscreteDiffusionSchedule:
    raise NotImplementedError()


def expected_mask_fraction(schedule: DiscreteDiffusionSchedule, timesteps: t.Tensor) -> float:
    raise NotImplementedError()


tests.test_linear_mask_schedule_and_expected_fraction(
    linear_mask_schedule,
    expected_mask_fraction,
)
```

<details><summary>Solution</summary>

```python
def linear_mask_schedule(num_steps, *, mask_token_id, min_mask_prob=0.0, max_mask_prob=1.0):
    if num_steps <= 0:
        raise ValueError("num_steps must be positive.")
    if not 0 <= min_mask_prob <= max_mask_prob <= 1:
        raise ValueError("mask probabilities must satisfy 0 <= min <= max <= 1.")
    mask_probs = t.linspace(min_mask_prob, max_mask_prob, num_steps)
    return DiscreteDiffusionSchedule(mask_probs=mask_probs, mask_token_id=mask_token_id)


def expected_mask_fraction(schedule, timesteps):
    probs = schedule.mask_probs.to(device=timesteps.device, dtype=t.float32)[timesteps]
    return probs.mean().item()
```
</details>

<details><summary>Expected output</summary>

```text
All tests in `test_linear_mask_schedule_and_expected_fraction` passed!
```
</details>

Common bug: indexing timesteps from high noise to low noise here. The schedule stores low-to-high noise; the sampler will iterate it in reverse.

<details><summary>Help - why the schedule is low-to-high</summary>

Forward noising asks "how corrupted should this training example be?" It is
natural to index that from clean to fully masked. Sampling runs the same ladder
backward: start at the noisiest endpoint, then reduce the mask fraction one step
at a time.

</details>


### Exercise - apply forward noising

> ```yaml
> Difficulty: easy
> Importance: high
>
> You should spend up to 5-10 mins on this exercise.
> ```

At timestep 0, no tokens should be masked. At the highest timestep in the default schedule, every token should be masked.

```python
def apply_forward_noising(
    input_ids: t.Tensor,
    timesteps: t.Tensor,
    schedule: DiscreteDiffusionSchedule,
    *,
    generator: t.Generator | None = None,
) -> NoisingResult:
    raise NotImplementedError()


tests.test_forward_noising_extremes_and_seeded_masks(
    apply_forward_noising,
    linear_mask_schedule,
)
```

<details><summary>Solution</summary>

```python
def apply_forward_noising(input_ids, timesteps, schedule, *, generator=None):
    if input_ids.ndim != 2:
        raise ValueError("input_ids must have shape (batch, seq).")
    if timesteps.shape != (input_ids.shape[0],):
        raise ValueError("timesteps must have shape (batch,).")
    if timesteps.min() < 0 or timesteps.max() >= schedule.num_steps:
        raise ValueError("timesteps are out of range for schedule.")
    probs = schedule.mask_probs.to(device=input_ids.device, dtype=t.float32)[timesteps]
    random_values = t.rand(input_ids.shape, generator=generator, device=input_ids.device)
    mask = random_values < probs[:, None]
    noisy = input_ids.clone()
    noisy[mask] = schedule.mask_token_id
    return NoisingResult(noisy_tokens=noisy, mask=mask, timesteps=timesteps)
```
</details>

<details><summary>Expected output</summary>

```text
All tests in `test_forward_noising_extremes_and_seeded_masks` passed!
```
</details>

Common bug: using one random draw per sequence. Noising should independently decide whether to mask each token.

<details><summary>Help - per-token noising vs per-sequence noising</summary>

A diffusion LM learns to fill arbitrary missing positions. If you flip one coin
for the whole sequence, every example is either mostly clean or fully masked and
the model never sees the mixed corruption patterns used by the sampler.

</details>


# Denoising Objective


### Exercise - compute masked denoising loss

> ```yaml
> Difficulty: medium
> Importance: high
>
> You should spend up to 10 mins on this exercise.
> ```

The model predicts the original tokens, but the loss should only use positions that were actually masked by the forward process.

```python
def masked_denoising_loss(logits: t.Tensor, target_ids: t.Tensor, mask: t.Tensor) -> t.Tensor:
    raise NotImplementedError()


tests.test_masked_denoising_loss_uses_only_masked_positions(masked_denoising_loss)
```

<details><summary>Solution</summary>

```python
def masked_denoising_loss(logits, target_ids, mask):
    if logits.shape[:-1] != target_ids.shape or target_ids.shape != mask.shape:
        raise ValueError("logits, target_ids, and mask shapes are incompatible.")
    if not mask.any():
        raise ValueError("masked_denoising_loss requires at least one masked token.")
    return F.cross_entropy(logits[mask.bool()].float(), target_ids[mask.bool()].long())
```
</details>

<details><summary>Expected output</summary>

```text
All tests in `test_masked_denoising_loss_uses_only_masked_positions` passed!
```
</details>

Common bug: averaging loss over every token. That lets unmasked tokens dominate the objective and turns the task into copying visible inputs.

<details><summary>Help - why unmasked tokens do not get loss</summary>

Unmasked tokens are context. If they contribute to cross entropy, the model can
look good by copying visible tokens instead of learning how to reconstruct the
missing ones. The loss should measure only positions that the forward process
actually hid.

</details>


# Remasking and Sampling


### Exercise - remask low-confidence positions

> ```yaml
> Difficulty: medium
> Importance: high
>
> You should spend up to 10-15 mins on this exercise.
> ```

Confidence remasking fills positions with argmax predictions, then masks the least confident positions for the next denoising step. Uniform remasking is a useful control.

```python
def token_entropy(logits: t.Tensor) -> t.Tensor:
    raise NotImplementedError()


def confidence_remask(
    logits: t.Tensor,
    current_tokens: t.Tensor,
    *,
    mask_token_id: int,
    next_mask_fraction: float,
) -> t.Tensor:
    raise NotImplementedError()


def uniform_remask(
    tokens: t.Tensor,
    *,
    mask_token_id: int,
    next_mask_fraction: float,
    generator: t.Generator | None = None,
) -> t.Tensor:
    raise NotImplementedError()


tests.test_confidence_remask_entropy_and_uniform_control(
    confidence_remask,
    token_entropy,
    uniform_remask,
)
```

<details><summary>Solution</summary>

```python
def token_entropy(logits):
    log_probs = F.log_softmax(logits.float(), dim=-1)
    probs = log_probs.exp()
    return -(probs * log_probs).sum(dim=-1)


def confidence_remask(logits, current_tokens, *, mask_token_id, next_mask_fraction):
    if not 0 <= next_mask_fraction <= 1:
        raise ValueError("next_mask_fraction must be in [0, 1].")
    probs = F.softmax(logits.float(), dim=-1)
    confidence, predictions = probs.max(dim=-1)
    new_tokens = predictions.to(dtype=current_tokens.dtype)
    _, seq_len = current_tokens.shape
    num_to_mask = int(round(next_mask_fraction * seq_len))
    if num_to_mask == 0:
        return new_tokens
    low_conf = confidence.topk(k=num_to_mask, dim=-1, largest=False).indices
    new_tokens.scatter_(1, low_conf, mask_token_id)
    return new_tokens


def uniform_remask(tokens, *, mask_token_id, next_mask_fraction, generator=None):
    num_to_mask = int(round(next_mask_fraction * tokens.shape[1]))
    if num_to_mask == 0:
        return tokens.clone()
    scores = t.rand(tokens.shape, generator=generator, device=tokens.device)
    chosen = scores.topk(k=num_to_mask, dim=-1, largest=False).indices
    remasked = tokens.clone()
    remasked.scatter_(1, chosen, mask_token_id)
    return remasked
```
</details>

<details><summary>Expected output</summary>

```text
All tests in `test_confidence_remask_entropy_and_uniform_control` passed!
```
</details>

Common bug: keeping the old token values in unmasked positions rather than filling from the current prediction. Diffusion sampling revises tokens.

<details><summary>Help - what confidence remasking controls</summary>

Confidence remasking is the toy version of "commit when confident, revisit when
uncertain." Uniform remasking is a useful control because it keeps the same mask
budget while removing the confidence signal.

</details>


### Exercise - sample from an oracle denoiser

> ```yaml
> Difficulty: medium
> Importance: high
>
> You should spend up to 10-15 mins on this exercise.
> ```

Before loading a real model, verify that the sampler can recover a known target when the model function always places high logit mass on the correct token.

```python
def diffusion_sampler(
    model_fn,
    *,
    shape: tuple[int, int],
    schedule: DiscreteDiffusionSchedule,
    temperature: float = 0.0,
    remask: str = "confidence",
    generator: t.Generator | None = None,
    device: t.device | None = None,
) -> tuple[t.Tensor, list[DenoisingStepStats]]:
    raise NotImplementedError()


tests.test_oracle_diffusion_sampler_recovers_target(
    diffusion_sampler,
    linear_mask_schedule,
)
```

<details><summary>Solution</summary>

```python
def diffusion_sampler(model_fn, *, shape, schedule, temperature=0.0, remask="confidence", generator=None, device=None):
    if device is None:
        device = schedule.mask_probs.device
    tokens = t.full(shape, schedule.mask_token_id, dtype=t.long, device=device)
    stats = []
    for step in reversed(range(schedule.num_steps)):
        logits = model_fn(tokens, step)
        predictions = logits.argmax(dim=-1)
        if step == 0:
            tokens = predictions.to(dtype=tokens.dtype)
        else:
            next_fraction = float(schedule.mask_probs[step - 1].item())
            tokens = confidence_remask(
                logits,
                predictions.to(dtype=tokens.dtype),
                mask_token_id=schedule.mask_token_id,
                next_mask_fraction=next_fraction,
            )
        mask_fraction = tokens.eq(schedule.mask_token_id).float().mean().item()
        stats.append(DenoisingStepStats(
            step=step,
            mask_fraction=mask_fraction,
            mean_entropy=token_entropy(logits).mean().item(),
            committed_fraction=1.0 - mask_fraction,
        ))
    return tokens, stats
```
</details>

<details><summary>Expected output</summary>

```text
All tests in `test_oracle_diffusion_sampler_recovers_target` passed!
```
</details>

Common bug: iterating the schedule low-to-high in the sampler. Sampling starts from high noise and moves toward low noise.

<details><summary>Help - why the sampler starts fully masked</summary>

The sampler is the reverse process. It begins with no observed target tokens and
then repeatedly predicts a full sequence, keeps the most confident positions,
and leaves the uncertain positions masked for a later denoising step.

</details>


# Denoising-Step Diagnostics


### Exercise - measure commitment time and trajectory shape

> ```yaml
> Difficulty: medium
> Importance: high
>
> You should spend up to 10-15 mins on this exercise.
> ```

Commitment time is the first stored trajectory index where a position is no longer masked. In a real model, you would store the token trajectory and selected hidden activations after each denoising step.

```python
def commitment_times(tokens_over_steps: t.Tensor, mask_token_id: int) -> t.Tensor:
    raise NotImplementedError()


def edit_distance(a: list[int], b: list[int]) -> int:
    raise NotImplementedError()


def validate_activation_trajectory(
    activations: list[t.Tensor],
    *,
    expected_steps: int,
    batch: int,
    seq_len: int,
) -> bool:
    raise NotImplementedError()


tests.test_commitment_edit_distance_and_activation_trajectory(
    commitment_times,
    edit_distance,
    validate_activation_trajectory,
)
```

<details><summary>Solution</summary>

```python
def commitment_times(tokens_over_steps, mask_token_id):
    if tokens_over_steps.ndim != 3:
        raise ValueError("tokens_over_steps must have shape (steps, batch, seq).")
    unmasked = tokens_over_steps.ne(mask_token_id)
    any_unmasked = unmasked.any(dim=0)
    first = unmasked.float().argmax(dim=0).long()
    return t.where(any_unmasked, first, t.full_like(first, -1))


def edit_distance(a, b):
    prev = list(range(len(b) + 1))
    for i, token_a in enumerate(a, start=1):
        cur = [i]
        for j, token_b in enumerate(b, start=1):
            cur.append(min(prev[j] + 1, cur[j - 1] + 1, prev[j - 1] + int(token_a != token_b)))
        prev = cur
    return prev[-1]


def validate_activation_trajectory(activations, *, expected_steps, batch, seq_len):
    if len(activations) != expected_steps:
        return False
    return all(act.shape[0] == batch and act.shape[1] == seq_len for act in activations)
```
</details>

<details><summary>Expected output</summary>

```text
All tests in `test_commitment_edit_distance_and_activation_trajectory` passed!
```
</details>

Common bug: reporting the denoising `step` value rather than the stored trajectory index. The two are reversed in this section's sampler.

<details><summary>Help - commitment time is not diffusion timestep</summary>

The stored trajectory index counts the rows you saved while sampling. The
diffusion timestep counts the schedule value, which runs backward. Commitment
time answers "when did this token first become unmasked in the visible
trajectory?"

</details>


# Tiny Diffusion LM


### Exercise - wrap a tiny bidirectional denoiser

> ```yaml
> Difficulty: medium
> Importance: medium
>
> You should spend up to 10-15 mins on this exercise.
> ```

The GPU path trains this model on a generated conditional copy-pair grammar. First verify the token, position, and timestep embeddings produce token-level logits.

```python
class TinyConditionalDiffusionLM(nn.Module):
    def __init__(
        self,
        *,
        vocab_size: int = 11,
        seq_len: int = 6,
        num_steps: int = 6,
        d_model: int = 96,
    ) -> None:
        super().__init__()
        self.seq_len = seq_len
        self.token_embed = nn.Embedding(vocab_size, d_model)
        self.position_embed = nn.Embedding(seq_len, d_model)
        self.time_embed = nn.Embedding(num_steps, d_model)
        layer = nn.TransformerEncoderLayer(
            d_model=d_model,
            nhead=4,
            dim_feedforward=2 * d_model,
            batch_first=True,
            activation="gelu",
        )
        self.transformer = nn.TransformerEncoder(layer, num_layers=2)
        self.unembed = nn.Linear(d_model, vocab_size)

    def forward(self, input_ids: t.Tensor, timesteps: t.Tensor) -> t.Tensor:
        raise NotImplementedError()


tests.test_tiny_conditional_diffusion_lm_forward_shape(TinyConditionalDiffusionLM)
```

<details><summary>Solution</summary>

```python
class TinyConditionalDiffusionLM(nn.Module):
    ...
    def forward(self, input_ids: t.Tensor, timesteps: t.Tensor) -> t.Tensor:
        positions = t.arange(self.seq_len, device=input_ids.device)
        hidden = (
            self.token_embed(input_ids)
            + self.position_embed(positions)[None, :, :]
            + self.time_embed(timesteps)[:, None, :]
        )
        return self.unembed(self.transformer(hidden))
```
</details>

<details><summary>Expected output</summary>

```text
All tests in `test_tiny_conditional_diffusion_lm_forward_shape` passed!
```
</details>

Common bug: using a causal decoder mask. This toy diffusion denoiser is bidirectional over the corrupted sequence.

<details><summary>Help - why the denoiser is bidirectional</summary>

Autoregressive models must not see future target tokens. A masked denoiser is
different: it receives the whole corrupted sequence and predicts the original
tokens at masked positions. The architecture can attend left and right because
the hidden information is removed by masking, not by a causal attention mask.

</details>


# GPU Verification and Full Experiment Path


The current CUDA report trains a tiny conditional diffusion LM on generated copy-pair sequences:

```text
[a, b] -> [a, b, a, a, b, b]
```

The first two tokens are protected prefix tokens. The suffix is masked and denoised. The report checks held-out masked-token accuracy, confidence-remasking sampler exact match, activation trajectory shapes, entropy over denoising steps, a shuffled-label negative control, and peak VRAM. This is a model-organism proof of mechanics, not released DiffusionGemma parity.

The same report also runs the released-model readiness contract:

```text
google/diffusiongemma-26B-A4B-it
  revision: 0f28bc42f588fbd8f71e08102b1c3960298a1358
  required BF16 shards: 11
  runtime target: Transformers DiffusionGemmaForBlockDiffusion

nvidia/diffusiongemma-26B-A4B-it-NVFP4
  revision: 2ea837236295d617ac27f8c17d61228081932c40
  required NVFP4 shards: 2
  runtime target: ModelOpt-compatible Transformers or vLLM NVFP4 inference
```

The report is allowed to say the config, tokenizer, and model class are supported. It is
not allowed to call that released-checkpoint generation. A real DiffusionGemma generation
claim requires successful local BF16 generation or a pinned NVFP4 proof that actually
consumes the checkpoint and executes generation.

The current 24GB path uses complete NVIDIA NVFP4 shards and an isolated vLLM `0.24.0`
runtime with torch `2.11.0+cu130`; the main uv environment remains torch `2.12.1+cu132`
and does not host vLLM. Use
`scripts/prepare_diffusiongemma_artifacts.py --artifact nvfp4 --download --require-local --max-workers 1`
to prepare the exact quantized artifact before rerunning the proof.

<details><summary>Expected GPU report highlights</summary>

```text
preflight_passed: true
heldout_masked_accuracy: >= 0.95
sampler_exact_match: >= 0.95
shuffled_label_accuracy: <= 0.25
activation_trajectory_shape_ok: true
peak_vram_gb: below 1GB for this tiny path
```
</details>

## Signature Result

The current accepted report was produced in the managed CUDA 13.2 `uv`
environment, plus an isolated vLLM runtime for the NVFP4 released-checkpoint
proof. The toy diffusion result and the released-checkpoint result are separate
claims.

| Check | Current result | Acceptance rule |
| --- | ---: | --- |
| Tiny denoising loss | `0.000045` | at most `0.05` |
| Held-out masked-token accuracy | `1.000` | at least `0.95` |
| Sampler exact-match rate | `1.000` | at least `0.95` |
| Suffix token accuracy | `1.000` | at least `0.95` |
| Shuffled-label accuracy | `0.071` | at most `0.25` |
| Activation trajectory shape | `true` | must be true |
| Mean suffix commitment step | `1.688` | recorded diagnostic |
| Tiny path peak VRAM | `0.074 GB` | below `24 GB` |
| BF16 direct loading on 24GB | deferred | 48.10 GiB of weights |
| NVFP4 local shards | `2 / 2` | all required shards present |
| NVFP4 isolated-vLLM generation | `true` | real CUDA generation proof |
| Isolated vLLM runtime | `torch 2.11.0+cu130`, `vLLM 0.24.0` | recorded exact runtime |

<details><summary>Interpreting the signature result</summary>

The tiny model result supports the diffusion-mechanics claim: the noising,
masked loss, confidence-remasking sampler, commitment diagnostics, and
shuffled-label control work on a generated conditional grammar. The NVFP4 result
supports a released-checkpoint feasibility claim: the pinned NVIDIA
DiffusionGemma artifact generated a non-empty answer locally through an isolated
vLLM runtime. It does not support a claim about released-checkpoint
denoising-step circuits or activation patching.

</details>

<details><summary>Expected denoising dynamics</summary>

```text
entropy_by_step:
  [0.0006, 0.0007, 0.0007, 0.0008, 0.0008, 0.0050]

commitment:
  suffix_commitment_mean_step = 1.688
```

The exact entropy values are tiny because the generated copy-pair grammar is
deliberately easy after training. In a harder notebook extension, this table
should become a plot where different token positions commit at different times.

</details>

## Limitations

This is a GT-0 mechanics and feasibility section. It does not claim
DiffusionGemma interpretability, denoising-time activation capture from the
released checkpoint, diffusion-time patching, throughput benchmarking, broad
quality evaluation, or BF16 direct inference on the 24GB machine. The real
checkpoint proof is one prompt through an isolated NVFP4 vLLM runtime. Treat it
as a local generation proof, not as a mechanistic result.


# Notebook Contract


This section exposes the standard extension entry points:

```python
def run_smoke_test(cpu: bool = True) -> dict:
    ...


def run_gpu_test(max_vram_gb: float = 24.0) -> dict:
    ...


def run_full_experiment(max_vram_gb: float = 24.0) -> dict:
    ...
```

The smoke test passes only if:

```text
linear schedule and expected mask fraction are correct
forward noising handles zero-noise and full-noise extremes
masked denoising loss is low for confident masked logits
confidence remasking masks the requested low-confidence positions
oracle sampler reconstructs the target sequence
trajectory diagnostics report commitment times and valid activation shapes
```

The remaining released-checkpoint work is narrower: the NVFP4 one-prompt generation proof exists, but it is not a diffusion-time interpretability exercise. Capture denoising-step outputs/activations from that run and add a diffusion-time patching exercise with a matched random-control intervention before claiming mechanism-level DiffusionGemma parity.
