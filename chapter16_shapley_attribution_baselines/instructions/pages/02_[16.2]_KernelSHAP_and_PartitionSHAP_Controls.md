# [16.2] KernelSHAP and PartitionSHAP Controls


> **Local-first extension.** This section continues the Shapley attribution
> baseline track. KernelSHAP is solved as a weighted regression over a complete
> coalition table, and PartitionSHAP/Owen values are checked against exact
> Shapley on games where the answer is known.

Please send any problems / bugs on the `#errata` channel in the [Slack group](https://info-arena.github.io/ARENA_img/slack.html), and ask any questions on the dedicated channels for this chapter of material.

If you want to change to dark mode, you can do this by clicking the three horizontal lines in the top-right, then navigating to Settings -> Theme.

Links to earlier chapters: [(0) Fundamentals](https://arena-chapter0-fundamentals.streamlit.app/), [(1) Transformer Interpretability](https://arena-chapter1-transformer-interp.streamlit.app/), [(2) RL](https://arena-chapter2-rl.streamlit.app/), [(3) LLM Evaluations](https://arena-chapter3-llm-evals.streamlit.app/), [(4) Alignment Science](https://arena-chapter4-alignment-science.streamlit.app/).

<img src="https://raw.githubusercontent.com/info-arena/ARENA_img/main/misc/headers/header-13-2.png" width="350">


## Metadata

```python
GT_TIER = "GT-0"
EXERCISE_ID = "16_2_kernelshap_and_partitionshap_controls"
EXPECTED_RUNTIME = "45-70 minutes for exercises; under 2 minutes for the CUDA finite-game report"
REQUIRES_GPU = True
```


# Introduction

KernelSHAP asks for Shapley values through a regression lens: represent each
coalition by a binary feature mask, weight rows by the KernelSHAP kernel, and
fit feature attributions subject to the efficiency constraint. With the full
coalition table available, this should recover exact Shapley values.

PartitionSHAP changes the game before assigning credit. It groups features and
computes Owen values by averaging over group orderings and within-group
orderings. This is useful for correlated tokens, image regions, and modality
groups, but the grouping assumption must be visible. A shortcut that computes
only within-group Shapley values can assign credit to irrelevant players when
the true interaction crosses group boundaries.

The checked CUDA path trains a finite four-feature neural coalition game,
builds coalition values from real model ablations, and runs these SHAP controls
on the resulting table. KernelSHAP must match exact Shapley and analytic ground
truth, singleton PartitionSHAP must recover exact credit, grouped PartitionSHAP
must use exact Owen values, a cross-group interaction counterexample must give
the irrelevant grouped player zero credit, and a shuffled-label trained model
must be rejected.


## Content & Learning Objectives

### 1. KernelSHAP weights

You will implement the finite KernelSHAP coalition weight.

> ##### Learning Objectives
>
> * Exclude empty and full coalitions from the finite-weight rows.
> * Use the combinatorial KernelSHAP weighting formula.
> * Raise clear errors for invalid coalition sizes.

### 2. KernelSHAP weighted regression

You will solve the constrained weighted linear system.

> ##### Learning Objectives
>
> * Build a binary coalition design matrix.
> * Fit weighted least squares with the KernelSHAP weights.
> * Enforce efficiency as an equality constraint.

### 3. KernelSHAP parity controls

You will compare KernelSHAP against exact Shapley on additive and interaction
games.

> ##### Learning Objectives
>
> * Recover additive feature weights exactly.
> * Split symmetric conjunction credit equally.
> * Treat approximation error as a reported metric, not an afterthought.

### 4. PartitionSHAP / Owen values

You will compute grouped credit and distribute it within each group.

> ##### Learning Objectives
>
> * Collapse player coalitions into group coalitions.
> * Compute group-level Shapley values.
> * Allocate group credit through exact within-group Shapley values.

### 5. CUDA neural-game SHAP controls

You will inspect the committed real CUDA report.

> ##### Learning Objectives
>
> * Distinguish full-table SHAP evidence from sampled large-model SHAP claims.
> * Check grouping assumptions with aligned and mismatched partitions.
> * Reject shuffled-label attributions as a negative control.


## Setup code

```python
import itertools
import math
import sys
from collections.abc import Callable, Mapping
from dataclasses import dataclass
from pathlib import Path

import torch as t

chapter = "chapter16_shapley_attribution_baselines"
section = "part2_kernelshap_partition_shap_controls"
root_dir = next(p for p in [Path.cwd(), *Path.cwd().parents] if (p / chapter).exists())
exercises_dir = root_dir / chapter / "exercises"
section_dir = exercises_dir / section

if str(root_dir) not in sys.path:
    sys.path.append(str(root_dir))
if str(exercises_dir) not in sys.path:
    sys.path.append(str(exercises_dir))

import part2_kernelshap_partition_shap_controls.tests as tests
import part2_kernelshap_partition_shap_controls.utils as utils

Coalition = frozenset[int]
MAIN = __name__ == "__main__"
```

```python
@dataclass(frozen=True)
class KernelSHAPApproximationReport:
    shapley_values: t.Tensor
    exact_values: t.Tensor
    max_abs_error: float
    approximates_exact: bool


@dataclass(frozen=True)
class PartitionSHAPReport:
    group_values: t.Tensor
    player_values: t.Tensor
    exact_values: t.Tensor
    max_abs_error: float
    recovers_exact: bool
```

The support helpers below are the same exact-game primitives you used in 16.1.
They are included here so this notebook does not import solved SHAP helpers.

```python
def all_coalitions(num_players: int) -> tuple[Coalition, ...]:
    if num_players <= 0:
        raise ValueError("num_players must be positive.")
    players = range(num_players)
    coalitions = []
    for size in range(num_players + 1):
        coalitions.extend(frozenset(group) for group in itertools.combinations(players, size))
    return tuple(coalitions)


def normalize_coalition_values(
    coalition_values: Mapping[Coalition | tuple[int, ...], float],
    *,
    num_players: int,
) -> dict[Coalition, float]:
    values = {frozenset(key): float(value) for key, value in coalition_values.items()}
    expected = set(all_coalitions(num_players))
    missing = expected - set(values)
    if missing:
        raise ValueError(f"coalition value table is missing {len(missing)} coalitions.")
    return values


def coalition_values_from_function(
    num_players: int,
    value_fn: Callable[[Coalition], float],
) -> dict[Coalition, float]:
    return {coalition: float(value_fn(coalition)) for coalition in all_coalitions(num_players)}


def exact_shapley_values(
    coalition_values: Mapping[Coalition | tuple[int, ...], float],
    *,
    num_players: int,
) -> t.Tensor:
    values = normalize_coalition_values(coalition_values, num_players=num_players)
    denominator = math.factorial(num_players)
    shapley = t.zeros(num_players, dtype=t.float64)
    for player in range(num_players):
        others = [item for item in range(num_players) if item != player]
        for size in range(num_players):
            weight = (
                math.factorial(size)
                * math.factorial(num_players - size - 1)
                / denominator
            )
            for group in itertools.combinations(others, size):
                coalition = frozenset(group)
                shapley[player] += weight * (
                    values[coalition | {player}] - values[coalition]
                )
    return shapley


def additive_game(weights: t.Tensor) -> dict[Coalition, float]:
    weights = weights.flatten().double()
    return coalition_values_from_function(
        int(weights.numel()),
        lambda coalition: weights[list(coalition)].sum().item() if coalition else 0.0,
    )


def conjunction_game(num_players: int) -> dict[Coalition, float]:
    full = frozenset(range(num_players))
    return coalition_values_from_function(num_players, lambda coalition: coalition == full)
```


# KernelSHAP weights


### Exercise - implement the finite KernelSHAP weight

> ```yaml
> Difficulty: medium
> Importance: high
>
> You should spend up to 10 mins on this exercise.
> ```

The finite KernelSHAP kernel for a coalition of size `s` among `n` players is

```text
(n - 1) / (binom(n, s) * s * (n - s))
```

It is only defined for non-empty, non-full coalitions. Empty and full coalitions
are handled through the efficiency constraint instead.

```python
def kernelshap_kernel_weight(coalition_size: int, num_players: int) -> float:
    raise NotImplementedError()


tests.test_kernelshap_kernel_weight_uses_finite_coalition_formula(
    kernelshap_kernel_weight,
)
```

Expected output:

```text
All tests in `test_kernelshap_kernel_weight_uses_finite_coalition_formula` passed!
```

Common bug: including the empty or full coalition as a finite-weight regression
row usually hides a broken efficiency constraint. Raise an error instead.

<details>
<summary>Solution</summary>

```python
def kernelshap_kernel_weight(coalition_size: int, num_players: int) -> float:
    if coalition_size <= 0 or coalition_size >= num_players:
        raise ValueError("KernelSHAP finite weights require 0 < coalition_size < num_players.")
    return (num_players - 1) / (
        math.comb(num_players, coalition_size)
        * coalition_size
        * (num_players - coalition_size)
    )
```

</details>


# KernelSHAP weighted regression


### Exercise - solve full-table KernelSHAP

> ```yaml
> Difficulty: hard
> Importance: high
>
> You should spend up to 20 mins on this exercise.
> ```

Build the coalition design matrix, weight the rows, and solve the KKT system
for weighted least squares with the efficiency constraint:

```text
sum_i phi_i = v(full) - v(empty)
```

```python
def kernelshap_values(
    coalition_values: Mapping[Coalition | tuple[int, ...], float],
    *,
    num_players: int,
) -> t.Tensor:
    raise NotImplementedError()


def kernelshap_approximation_report(
    coalition_values: Mapping[Coalition | tuple[int, ...], float],
    *,
    num_players: int,
    tolerance: float = 1e-9,
) -> KernelSHAPApproximationReport:
    raise NotImplementedError()


tests.test_kernelshap_approximation_report_matches_exact_additive_game(
    kernelshap_approximation_report,
    additive_game,
)
```

Expected output:

```text
All tests in `test_kernelshap_approximation_report_matches_exact_additive_game` passed!
```

Common bug: solving an unconstrained regression can look plausible on some
tables but fail efficiency. Use a one-row equality constraint in the KKT solve.

<details>
<summary>Solution</summary>

```python
def kernelshap_values(
    coalition_values: Mapping[Coalition | tuple[int, ...], float],
    *,
    num_players: int,
) -> t.Tensor:
    if num_players < 2:
        raise ValueError("KernelSHAP needs at least two players for finite weights.")
    values = normalize_coalition_values(coalition_values, num_players=num_players)
    empty = frozenset()
    full = frozenset(range(num_players))
    baseline = values[empty]
    total_delta = values[full] - baseline

    rows = []
    targets = []
    weights = []
    for coalition in all_coalitions(num_players):
        size = len(coalition)
        if size == 0 or size == num_players:
            continue
        rows.append([1.0 if player in coalition else 0.0 for player in range(num_players)])
        targets.append(values[coalition] - baseline)
        weights.append(kernelshap_kernel_weight(size, num_players))

    design = t.tensor(rows, dtype=t.float64)
    target = t.tensor(targets, dtype=t.float64)
    weight_vector = t.tensor(weights, dtype=t.float64)
    hessian = design.T @ (design * weight_vector.unsqueeze(-1))
    rhs = design.T @ (target * weight_vector)
    constraint = t.ones((1, num_players), dtype=t.float64)
    kkt = t.cat(
        [
            t.cat([hessian, constraint.T], dim=1),
            t.cat([constraint, t.zeros((1, 1), dtype=t.float64)], dim=1),
        ],
        dim=0,
    )
    constrained_rhs = t.cat([rhs, t.tensor([total_delta], dtype=t.float64)])
    return t.linalg.solve(kkt, constrained_rhs)[:num_players]


def kernelshap_approximation_report(
    coalition_values: Mapping[Coalition | tuple[int, ...], float],
    *,
    num_players: int,
    tolerance: float = 1e-9,
) -> KernelSHAPApproximationReport:
    kernel_values = kernelshap_values(coalition_values, num_players=num_players)
    exact = exact_shapley_values(coalition_values, num_players=num_players)
    max_error = float((kernel_values - exact).abs().max().item())
    return KernelSHAPApproximationReport(
        shapley_values=kernel_values,
        exact_values=exact,
        max_abs_error=max_error,
        approximates_exact=max_error <= tolerance,
    )
```

</details>


### Exercise - test KernelSHAP on an interaction game

> ```yaml
> Difficulty: medium
> Importance: high
>
> You should spend up to 10 mins on this exercise.
> ```

The three-player conjunction game has no single-feature main effect, but
Shapley still assigns one third of the full value to each player.

```python
tests.test_kernelshap_interaction_report_splits_conjunction_credit(
    kernelshap_approximation_report,
    conjunction_game,
)
```

Expected output:

```text
All tests in `test_kernelshap_interaction_report_splits_conjunction_credit` passed!
```

Common bug: treating KernelSHAP as leave-one-out attribution gives each player
too much credit in interaction games.


# PartitionSHAP / Owen values


### Exercise - collapse player coalitions into group coalitions

> ```yaml
> Difficulty: medium
> Importance: high
>
> You should spend up to 10 mins on this exercise.
> ```

PartitionSHAP first forms a game over groups. A group coalition is converted
back into the union of its original players before reading the value table.

```python
def grouped_coalition_values(
    coalition_values: Mapping[Coalition | tuple[int, ...], float],
    *,
    groups: tuple[tuple[int, ...], ...],
) -> dict[Coalition, float]:
    raise NotImplementedError()
```

Expected output:

```text
For weights [1, 2, 3, 4] and groups ((0, 1), (2, 3)), group Shapley is [3.0, 7.0].
```

Common bug: using group indices as player indices without expanding the groups
will silently compute values for the wrong coalition.

<details>
<summary>Solution</summary>

```python
def grouped_coalition_values(
    coalition_values: Mapping[Coalition | tuple[int, ...], float],
    *,
    groups: tuple[tuple[int, ...], ...],
) -> dict[Coalition, float]:
    group_count = len(groups)
    player_count = sum(len(group) for group in groups)
    values = normalize_coalition_values(coalition_values, num_players=player_count)
    group_values = {}
    for group_coalition in all_coalitions(group_count):
        players: set[int] = set()
        for group_index in group_coalition:
            players.update(groups[group_index])
        group_values[group_coalition] = values[frozenset(players)]
    return group_values
```

</details>


### Exercise - compute PartitionSHAP player values

> ```yaml
> Difficulty: hard
> Importance: high
>
> You should spend up to 20 mins on this exercise.
> ```

Compute exact Shapley over the group game, then distribute each group value
using exact Shapley inside that group. For zero within-group total value, split
the group value uniformly; otherwise scale the local within-group Shapley
values so they sum to the group value.

```python
def partition_shap_values(
    coalition_values: Mapping[Coalition | tuple[int, ...], float],
    *,
    groups: tuple[tuple[int, ...], ...],
) -> t.Tensor:
    raise NotImplementedError()


def partition_shap_report(
    coalition_values: Mapping[Coalition | tuple[int, ...], float],
    *,
    groups: tuple[tuple[int, ...], ...],
    tolerance: float = 1e-9,
) -> PartitionSHAPReport:
    raise NotImplementedError()


tests.test_partition_shap_report_recovers_additive_groups(
    partition_shap_report,
    additive_game,
)
tests.test_partition_shap_report_rejects_invalid_grouping(
    partition_shap_report,
    additive_game,
)
```

Expected output:

```text
All tests in `test_partition_shap_report_recovers_additive_groups` passed!
All tests in `test_partition_shap_report_rejects_invalid_grouping` passed!
```

Common bug: allowing overlapping groups makes the result uninterpretable.
Require the groups to partition players `0..n-1` exactly once.

<details>
<summary>Solution</summary>

```python
def partition_shap_values(
    coalition_values: Mapping[Coalition | tuple[int, ...], float],
    *,
    groups: tuple[tuple[int, ...], ...],
) -> t.Tensor:
    if not groups:
        raise ValueError("groups must be nonempty.")
    flattened = [player for group in groups for player in group]
    if sorted(flattened) != list(range(len(flattened))):
        raise ValueError("groups must partition players 0..n-1 exactly once.")

    values = normalize_coalition_values(coalition_values, num_players=len(flattened))
    group_values = grouped_coalition_values(values, groups=groups)
    group_shapley = exact_shapley_values(group_values, num_players=len(groups))
    player_values = t.zeros(len(flattened), dtype=t.float64)

    for group_index, group in enumerate(groups):

        def within_value(coalition: Coalition) -> float:
            selected_players = {group[player] for player in coalition}
            return values[frozenset(selected_players)] if selected_players else 0.0

        local_values = coalition_values_from_function(len(group), within_value)
        local_total = local_values[frozenset(range(len(group)))] - local_values[frozenset()]
        if abs(local_total) <= 1e-12:
            for player in group:
                player_values[player] = group_shapley[group_index] / len(group)
            continue

        local_shapley = exact_shapley_values(local_values, num_players=len(group))
        scale = group_shapley[group_index] / local_total
        for local_index, player in enumerate(group):
            player_values[player] = local_shapley[local_index] * scale
    return player_values


def partition_shap_report(
    coalition_values: Mapping[Coalition | tuple[int, ...], float],
    *,
    groups: tuple[tuple[int, ...], ...],
    tolerance: float = 1e-9,
) -> PartitionSHAPReport:
    num_players = sum(len(group) for group in groups)
    player_values = partition_shap_values(coalition_values, groups=groups)
    exact = exact_shapley_values(coalition_values, num_players=num_players)
    group_values = exact_shapley_values(
        grouped_coalition_values(coalition_values, groups=groups),
        num_players=len(groups),
    )
    max_error = float((player_values - exact).abs().max().item())
    return PartitionSHAPReport(
        group_values=group_values,
        player_values=player_values,
        exact_values=exact,
        max_abs_error=max_error,
        recovers_exact=max_error <= tolerance,
    )
```

</details>


### Exercise - split a pure interaction group symmetrically

> ```yaml
> Difficulty: medium
> Importance: high
>
> You should spend up to 10 mins on this exercise.
> ```

When both players are inside one pure conjunction group, the within-group
credit should split equally.

```python
tests.test_partition_shap_report_splits_interaction_group_symmetrically(
    partition_shap_report,
    conjunction_game,
)
```

Expected output:

```text
All tests in `test_partition_shap_report_splits_interaction_group_symmetrically` passed!
```

Common bug: assigning all group value to the first player gives the right group
total but destroys player-level attribution.


# Notebook contract


```python
def _tensor_report(report: object) -> dict:
    result = report.__dict__.copy()
    for key, value in list(result.items()):
        if hasattr(value, "tolist"):
            result[key] = value.tolist()
    return result


def kernel_additive_smoke_test() -> dict:
    values = additive_game(t.tensor([1.0, -2.0, 0.5]))
    return _tensor_report(kernelshap_approximation_report(values, num_players=3))


def kernel_interaction_smoke_test() -> dict:
    values = conjunction_game(3)
    return _tensor_report(kernelshap_approximation_report(values, num_players=3))


def partition_additive_smoke_test() -> dict:
    values = additive_game(t.tensor([1.0, 2.0, 3.0, 4.0]))
    return _tensor_report(partition_shap_report(values, groups=((0, 1), (2, 3))))


def partition_interaction_smoke_test() -> dict:
    values = conjunction_game(2)
    return _tensor_report(partition_shap_report(values, groups=((0, 1),)))


def run_smoke_test(cpu: bool = True) -> dict:
    _ = cpu
    return {
        "kernel_additive": kernel_additive_smoke_test(),
        "kernel_interaction": kernel_interaction_smoke_test(),
        "partition_additive": partition_additive_smoke_test(),
        "partition_interaction": partition_interaction_smoke_test(),
    }


tests.test_notebook_contract(run_smoke_test)
```

Expected output:

```text
All tests in `test_notebook_contract` passed!
```

Common bug: returning tensors directly from the smoke test makes report JSON
serialization fail. Convert tensors to lists at the smoke-test boundary.


# CUDA verification report

This section is GT-0. The report proves full-table SHAP controls on a finite
trained model organism; it does not claim sampled SHAP reliability on large
language or vision models.

```python
import json

report = json.loads((section_dir / "verification_report.json").read_text())
gpu = report["metrics"]["gpu_test"]

assert report["accepted"] is True
assert gpu["cuda_available"] is True
assert gpu["preflight_passed"] is True
assert gpu["kernel_approximates_exact"] is True
assert gpu["kernel_vs_true_max_abs_error"] <= 1e-4
assert gpu["partition_recovers_exact"] is True
assert gpu["aligned_partition_recovers_exact"] is True
assert gpu["mismatched_partition_recovers_exact"] is True
assert gpu["cross_group_partition_recovers_exact"] is True
assert gpu["cross_group_irrelevant_credit"] <= 1e-8
assert gpu["shuffled_control_rejected"] is True
assert gpu["within_vram_budget"] is True
```

Expected output:

```text
{
  "device": "NVIDIA GeForce RTX 5090 Laptop GPU",
  "kernel_vs_true_max_abs_error": <= 0.0001,
  "partition_recovers_exact": true,
  "cross_group_irrelevant_credit": <= 1e-8,
  "shuffled_control_rejected": true,
  "peak_vram_gb": <= 1.0
}
```

Common bug: distributing group-level credit using only within-group coalitions.
That shortcut passes additive examples but gives irrelevant players credit when
an interaction crosses group boundaries.
