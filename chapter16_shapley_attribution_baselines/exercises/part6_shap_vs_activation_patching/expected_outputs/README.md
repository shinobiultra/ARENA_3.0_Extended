# [16.6] SHAP vs Activation Patching Expected Outputs

This directory contains frozen fixtures for the learner-facing notebook contract. These fixtures protect visible exercise behavior; the exact numerical oracle below is the trusted learner result. Supporting CUDA evidence is pinned in `artifacts.lock.yml` and measured separately in `verification_report.json`.

## How this fixture was produced

The generic `smoke_test.json` schema is generated by `scripts/generate_extension_verification_assets.py`. The section-specific values in `reference_metrics.json` were produced by the checked-in `solutions.py::run_smoke_test(cpu=True)` exact-organism path and cross-checked by `tests.py`. Section verification reports are regenerated by `scripts/run_extension_verification_reports.py`.

## Trusted implementation

The exact forward pass is `score = 1.0*RED + 0.6*SQUARE + 0.25*BRIGHT + 2.4*ReLU(RED + SQUARE - 1)`. Clean activations are all ones and corrupt activations are all zeros. This gives token Shapley `[2.2, 1.8, 0.25, 0.0]`, token noising patch losses `[3.4, 3.0, 0.25, 0.0]`, and post-ReLU hidden values `[1.0, 0.6, 0.25, 0.0, 2.4]` for both methods.

## Random seed

The exact result is deterministic. Matched random hidden directions use seed `1666` and `512` samples; their mean absolute effect is `0.9943479121`, p95 is `2.1658278195`, and the true gate effect is at percentile `0.986328125`.

## Allowed tolerances

Integer, label, boolean, and coalition checks require exact equality. The float64 toy oracle uses `rtol=0` and `atol=1e-12`. The supporting trained CUDA path retains float32 tolerances declared in `artifacts.lock.yml`.

## When to regenerate

Regenerate these fixtures when the section title, GT tier, artifact lock schema, verification report schema, expected baseline/control slots, or trusted toy implementation changes. Do not regenerate merely to hide a failing check; update the implementation or the declared claim scope first.
