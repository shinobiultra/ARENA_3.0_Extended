# Method Cards

Write one method card per method that appears in the project.

Each card should answer:

- what the method claims to measure
- what inputs it consumes
- what outputs it produces
- what baseline it is compared against
- what negative control should fail
- what result would make the method useless here

Start from `template.md`.
