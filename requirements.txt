# PyTorch stable CUDA 13.2 stack, verified with uv on Python 3.14.6.
# The authoritative extension environment is now pyproject.toml + uv.lock:
#   BNB_CUDA_VERSION=130 uv sync --locked
# Direct torch-only repair path, if needed:
#   uv pip install --python .venv/bin/python torch torchvision --index-url https://download.pytorch.org/whl/cu132
#
# torchaudio is intentionally omitted here: the stable cu132 index currently has no
# Python 3.14 ABI wheel, while torch and torchvision do.
--index-url https://download.pytorch.org/whl/cu132
--extra-index-url https://pypi.org/simple
torch==2.12.1+cu132
torchvision==0.27.1+cu132
wheel==0.47.0

# ML & DL
huggingface_hub
torchtext
torchinfo
diffusers
wandb # ==0.13.10
accelerate
ftfy
safetensors
openai>=1.56.2
sentence-transformers
peft
# Current wheel ships CUDA 13.0, not 13.2; use BNB_CUDA_VERSION=130 locally.
bitsandbytes
# Mamba fast path: Transformers uses these CUDA extensions when available.
# For this Python 3.14 + torch cu132 stack these may build from source; if uv
# build isolation cannot see torch, install them with `uv pip install
# --no-build-isolation mamba-ssm causal-conv1d` after torch is installed.
kernels==0.12.3
mamba-ssm==2.3.2.post1
causal-conv1d==1.6.2.post1

# Data manipulation and analysis
numpy==1.26.4  # circuitsvis currently declares numpy<2 on Python >=3.10
einops
eindex-callum
pandas
numpy-stl
scikit-learn
hdbscan
umap-learn

# Utilities
requests
fsspec==2025.3.0  # pinned by s3fs/datasets/inspect-ai compatibility
pillow==11.3.0  # moviepy currently declares pillow<12
pytest
tqdm
ipykernel
notebook
ipywidgets==7.7.1
jupyterlab-widgets==1.1.1
jupyter
pre-commit
gdown
portalocker>=2.0.0
typeguard
jaxtyping
tiktoken
frozendict
tabulate
opencv-python
nbformat
docformatter
pyyaml

# Visualization
matplotlib
plotly
streamlit
streamlit-antd-components==0.2.5
streamlit-image-select
streamlit-on-Hover-tabs
nnsight>=0.3.5
imageio-ffmpeg
mediapy
captum==0.9.0
shap==0.49.1
shapiq==1.5.2
# inseq is isolated for now: current releases pin typeguard<=2.13.3, which
# conflicts with TransformerLens 2.18.0 on the default Python 3.14/CUDA13 env.

# Chapter 1 (interp)
transformer_lens>=2.16.1,<3.0.0
transformers>=4.51,<5
sae-lens>=6.37.4,<7.0.0
sae-vis==0.3.7
git+https://github.com/callummcdougall/CircuitsVis.git#subdirectory=python
git+https://github.com/neelnanda-io/neel-plotly

# Chapter 2 (RL)
pygame
gymnasium[other]==0.29.0
# The older Brax/EnvPool/MuJoCo rendering stack is isolated from this Python
# 3.14 + CUDA 13.2 default environment; see requirements-legacy-rl.txt.

# Chapter 3 (evals)
inspect_ai
tabulate
wikipedia
python-dotenv
anthropic>=0.42.0
instructor
